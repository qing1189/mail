# Docker 环境变量配置

# 时区设置
TZ=Asia/Shanghai

# 是否启用 VNC（用于调试查看浏览器界面）
# true - 启用 VNC，可通过 VNC 客户端连接 localhost:5900
# false - 禁用 VNC（默认）
ENABLE_VNC=false

# Python 输出不缓冲（确保日志实时显示）
PYTHONUNBUFFERED=1

# Web 面板密码（默认: admin123）
WEB_PASSWORD=admin123

# Token 有效期（小时，默认: 24）
TOKEN_EXPIRE_HOURS=24
